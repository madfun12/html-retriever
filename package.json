{
  "name": "html-retriever",
  "version": "1.0.0",
  "description": "A simple program that retrieves HMTL from a given url and rewrites it to a local file",
  "main": "retrieve-html.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3"
  }
}
